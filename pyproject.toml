[project]
name = "agent-hands-on"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "anthropic>=0.45.2",
    "langchain-anthropic>=0.3.7",
    "langgraph>=0.2.70",
]
